import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'com.phoneappdeck.timerstarter',
  appName: 'Study Timer',
  webDir: 'dist',
}

export default config
